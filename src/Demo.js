/**
 * function này để test thử
 *
 * @param {string} name
 * @param {function} method
 * @param {string|null} message
 */
 export function run(name = 'hùng đẹp trai') {
    console.log("echo ra name ", name)
}